//! LockBox — re-exported from `rivers-lockbox-engine`.
//!
//! When the `lockbox` feature is enabled, this module re-exports all
//! lockbox types and functions from the `rivers-lockbox-engine` crate.

pub use rivers_lockbox_engine::*;
